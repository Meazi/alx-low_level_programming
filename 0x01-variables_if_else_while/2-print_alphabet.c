#include <stdio.h>
#include <stdlib.h>
/**
* main - Entry point
*
* Return: Always 0 (Successi)
*/
int main(void)
{
int ch='a';
		while (ch <= 'z')
		{
			putchar(ch);
			putchar('\n');
			ch++;
		}
return (0);
}
